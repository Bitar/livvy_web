import React from "react";
import {LivButton} from "./components/buttons/LivButton.tsx";

export const Test = () => {

    return <>
        <LivButton text={'login'} borderColor={'border-black'} bgColor={'bg-black'} textColor={'text-white'} />
    </>
}